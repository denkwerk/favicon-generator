# Copy to .env.local (gitignored) and fill in. Used by the examples in this repository.

# Figma personal access token with the `file_content:read` scope, for examples/cli-figma
# (Figma → Settings → Security → Personal access tokens).
FIGMA_TOKEN=
