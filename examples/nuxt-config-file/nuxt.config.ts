// No `favicon` options here: the module reads favicon.config.ts.
export default defineNuxtConfig({
  modules: ['@denkwerk/nuxt-favicon-generator'],
  compatibilityDate: '2026-09-01',
  devtools: { enabled: false },
})
